name: Keep Render Alive (heartbeat)

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes
  workflow_dispatch:        # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Mysterium Status Proxy
        run: |
          echo "$(date -u) - Sending heartbeat..."
          curl --fail --silent --show-error --max-time 15 "https://mysterium-status-proxy.onrender.com/status/mysterium" >/dev/null
          echo "✅ Ping successful."
